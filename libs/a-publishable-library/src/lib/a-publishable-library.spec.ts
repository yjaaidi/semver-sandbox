import { aPublishableLibrary } from './a-publishable-library';

describe('aPublishableLibrary', () => {
  it('should work', () => {
    expect(aPublishableLibrary()).toEqual('a-publishable-library');
  });
});
